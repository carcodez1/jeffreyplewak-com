# Eval Stub

Scenario:
- The user asks for a new `/projects/*` proof page section tied to an external source repository.

Pass if:
- the workflow requires claim/source inspection before patching
- route creation or scope expansion is treated as approval-gated
- the page stays evidence-backed and does not imply this repo owns an external codebase
